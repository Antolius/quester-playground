package com.example.josip.engine.area;

import com.example.josip.engine.Processor;
import com.example.josip.model.Checkpoint;

public class AreaProcessor implements Processor {

    private AreaProcessedCallback areaProcessedCallback;

    @Override
    public void process(Checkpoint checkpoint) {
        //missing the location info
    }
}
