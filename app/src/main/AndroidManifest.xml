<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.josip.jstest" >

    <permission
        android:name="com.example.josip.jstest.activities.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="com.example.josip.jstest.activities.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyChgJlCFF1cq2uvvsAXcCuH3vfQITjIKho" />

        <activity
            android:name="com.example.josip.location.LocationTestActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MapBrowseActivity"
            android:label="Map Browser" >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <service
            android:name="com.example.josip.location.SendMockLocationsService"
            android:icon="@drawable/ic_launcher"
            android:label="Location test service" >
        </service>

        <service
            android:name="com.example.josip.gameService.GameEngineService"
            android:icon="@drawable/ic_launcher"
            android:label="Test service" >
        </service>

        <service
            android:name="com.example.josip.location.GeofenceIntent"
            android:label="@string/app_name"
            android:exported="false">
        </service>

        <activity
            android:name=".activities.MyActivity"
            android:label="@string/title_activity_location_test" >
        </activity>
    </application>

</manifest>
